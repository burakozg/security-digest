# Pinned, reproducible dependency snapshot -- generated from requirements.in.
# Do not hand-edit. To upgrade: edit requirements.in, then regenerate with
#   uv pip compile requirements.in -o requirements.txt
# then re-test before committing (this file pins exact versions that have
# actually been run against the app, not just "latest that satisfies >=").
#
# Install with: pip install -r requirements.txt

# --- Direct dependencies (from requirements.in) ---
feedparser==6.0.12
httpx==0.28.1
PyYAML==6.0.3
openai==2.26.0
anthropic==0.116.0
python-dotenv==1.2.2
markdown==3.10.2
fastapi==0.135.1
uvicorn==0.41.0
apscheduler==3.11.2

# --- Transitive dependencies (pinned for reproducibility) ---
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.12.1
certifi==2026.2.25
click==8.3.1
distro==1.9.0
docstring_parser==0.18.0
h11==0.16.0
httpcore==1.0.9
idna==3.11
jiter==0.13.0
pydantic==2.12.5
pydantic_core==2.41.5
sgmllib3k==1.0.0
sniffio==1.3.1
starlette==0.52.1
tqdm==4.67.3
typing-inspection==0.4.2
typing_extensions==4.15.0
tzlocal==5.3.1
